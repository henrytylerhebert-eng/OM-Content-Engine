# Model Layer

These models define the normalized domain shape for OM Content Engine.

The first pass keeps the models simple:

- readable field names
- optional fields where the source is messy
- traceability back to source rows
- minimal assumptions about the final database

Several fields that will likely become richer later, especially tags, are plain strings for now. That is deliberate.

